# MongoDB Connection String
# Example: mongodb+srv://<username>:<password>@cluster0.mongodb.net/?appName=Cluster0
MONGODB_URI=

# JWT Secret for Admin Authentication
# Change this to a secure random string in production
JWT_SECRET=SudipRo@2026Portfolio_SecretKey_123

# Port for the server (default is 3000)
PORT=3000

# Email Configuration (Nodemailer)
# Used for sending email notifications when the contact form is submitted
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_TO=destination-email@example.com
